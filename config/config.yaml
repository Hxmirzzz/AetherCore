app:
  name: "AetherCore"
  version: "4.0.0"

service:
  poll_interval_seconds: 30
  batch_size: 100
  max_retries: 3

logging:
  level: "INFO"
  format: "%(asctime)s - %(name)s - %(levelname)s - %(message)s"